services:
  portfolio-tracker:
    build: .
    container_name: portfolio-tracker
    restart: unless-stopped
    ports:
      - "3069:3069"
    volumes:
      - ./cache:/app/cache
      - ./data:/app/data
    environment:
      - PORT=3069
      - DATA_DIR=/app/data
